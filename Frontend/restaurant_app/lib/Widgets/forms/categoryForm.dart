import 'package:flutter/material.dart';

class CategoriesForm extends StatelessWidget {
  const CategoriesForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CategoriesForm'),
      ),
    );
  }
}
