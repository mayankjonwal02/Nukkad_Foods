import 'package:flutter/material.dart';

class DishesForm extends StatelessWidget {
  const DishesForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Dishes'),
      ),
    );
  }
}
