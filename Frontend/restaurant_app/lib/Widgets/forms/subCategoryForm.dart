import 'package:flutter/material.dart';

class SubCategoriesForm extends StatelessWidget {
  const SubCategoriesForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SubCategoriesForm'),
      ),
    );
  }
}
